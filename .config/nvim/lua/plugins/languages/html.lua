local M = {}

function M.load(use)
    -- treesitter.luaに移動
    -- -- HTML入力時の補助
    -- use {
    --     "windwp/nvim-ts-autotag",
    --     requires = 'nvim-treesitter/nvim-treesitter',
    --     config = function()
    --         require("nvim-ts-autotag").setup()
    --     end
    -- }
end

return M;
